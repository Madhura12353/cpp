// conversion of hexa to decimal compairing ASCII 
#include <iostream>
#include <string>
#include "math.h"
using namespace std;
unsigned long hex2dec(string hex)
{
	unsigned long result = 0;
	for (int i = 0; i<hex.length(); i++) {
		if (hex[i] >= 48 && hex[i] <= 57)
		{
			result += (hex[i] - 48)*pow(16, hex.length() - i - 1);
		}
		else if (hex[i] >= 65 && hex[i] <= 70) {
			result += (hex[i] - 55)*pow(16, hex.length() - i - 1);
		}
		else if (hex[i] >= 97 && hex[i] <= 102) {
			result += (hex[i] - 87)*pow(16, hex.length() - i - 1);
		}
	}
	return result;
}

int main(int argc, const char * argv[])
{
	string hex_str;
	cout << "Enter the hexa number: ";
	cin >> hex_str;
	cout << "This is the hexa to decimal conversion:";
	cout << hex2dec(hex_str) << endl;
	system("pause");
	return 0;
}

/*convert from an other base to decimal
Ask what the source base is (accept only integer input in the range [2, 36]). 
Read a number in that range. 
Make sure that the input is valid (contains only digits that are valid for that base).
Accept both upper and lower case input, for example if base is 16 the number 1b is legal as is the number 1B.
Note that since your number may contains digits that are letters you need to store the input in a character array or a char vector.
Do the conversion.

*/
/*#include <iostream>
using namespace std;

int conversion(int a, int base)

{
	
		int ans = a % 10;

		for (int i = base; (a /= 10) != 0; i *= base)
		 ans += a*i;
		return ans;
	
}

int main()
{
	
		int a, base, ans;
	

		
		cout << "Enter a number: ";
	
		cin >> a;
		
		cout << "Enter a base: ";
	
		cin >> base;
		while (base > 36 || base < 2)
		{
			cout << "Ilegeal base try again";
			cin >> base;
		}
	
		ans = conversion(a, base);
		
		cout << "the converted base 10 is" << ans;
	
		system("pause");		
	
}*/



/*
* C++ Program to Convert a Decimal Base to Any Base
*/
/*#include<iostream>
#include <cstdio>
using namespace std;*/

/*
* Convert a Decimal Base to Any Base

void convert10tob(int N, int b)
{
	if (N == 0)
		return;
	int x = N % b;
	N /= b;
	if (x < 0)
		N += 1;
	convert10tob(N, b);
	cout << (x < 0 ? x + (b * -1) : x);
	return;
}

/*
* Main
/*
/int main()
{
	int N, b;
	cout << "Enter the integer to convert(N): ";
	cin >> N;
	cout << "Enter the base <= N: ";
	cin >> b;
	if (N != 0)
	{
		convert10tob(N, b);
		cout << endl;
	}
	else
		cout << "0" << endl;
	
	system("pause");
}*/
